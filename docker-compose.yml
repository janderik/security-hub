version: '3.8'

services:
  security-hub:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///data/hub.db
    volumes:
      - ./data:/data
      - ./config:/app/config
